Default.mk: TTHREADS_HOME can be changed to specific tthreads home if you didn't download the whole package.
tests/defines.mk: should be changed to where the datasets located if you are not put them into ./datasets.

